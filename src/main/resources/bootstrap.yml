spring:
  application:
    name: ui-server

---

spring:
  profiles: development
  cloud:
    config:
      uri: http://127.0.0.1:8888

---

spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888

